---
title: "Shiny Survey Results"
author: "Shiny Survey Team"
date: "11/6/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Opinions about fruit


```{r getdatasetup, include=FALSE, echo=FALSE}
#install.packages("ggplot2")
library(ggplot2)
library(googledrive)



#setwd("/home/student17/Shinyrep") 

# The following command works when running in the console, but not yet from markdown, because you need to log in by hand. Probably use drive_auth(email = "shiny.h4ss@gmail.com") later?
#drive_download("~/response.csv", overwrite = TRUE)


df <- read.csv(file = 'response.csv')

  
```


This survey was completed by `r nrow(df)` participants.


```{r question1, echo=FALSE}

# Colors
fruitcolors=c("#86D76E", "#E3F25A", "#913838","#E8A22B")

p1 <- ggplot(df, aes(x = Favorite.fruit...SC, fill = Favorite.fruit...SC)) +
  geom_bar()+
  theme_bw()+
  scale_fill_manual(values= fruitcolors)+
  theme(legend.position = "none")+ 
  xlab("Fruit")+
  ylab("Number of Participants")+
  labs(title = "What is your favourite fruit?")

p1

```
